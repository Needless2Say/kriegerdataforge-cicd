======================================== PROMPT GENERATOR — FRONTEND TEMPLATE SKELETON REQUEST ========================================

Prompt Name: Create Template Front End Skeleton

Target AI Role: [X] 🏗️ ARCHITECT

Project/Application Name: Front End Template Skeleton

Brief Description of Project:
The Front End Template Skeleton is a folder that should be a template skeleton of a Next.js
Frontend application that can be used to create an application without having to do all the
setup for the front end application repo.

This template skeleton should use the fitness app frontend application as an example for how
to structure the template skeleton so developers can create other applications with the same
framework structure.

Any developer should be able to copy this frontend template skeleton and immediately get
started on developing their frontend application.

It should include the authentication system already baked into the template code because every
application in the KriegerDataForge ecosystem will connect to the authentication system in the
FastAPI backend and authenticate through that.

It should also include examples of how to structure features with example files and other types
of examples to show how to use the template. There should also be documentation carried over
from the fitness app frontend application that is generalized for the template skeleton, such
as coding standards, architecture standards, etc that is general enough to be copied over and
used for any frontend application in the KriegerDataForge ecosystem.

The frontend template skeleton should be placed in the templates directory.

Scope: [X] 🏢 Enterprise - Organization-wide template for all future front-end applications

Tech Stack:
  Frontend:  Next.js | TailwindCSS | TypeScript/React | React state
  Backend:   FastAPI | Python/SQL | PostgreSQL | SQLAlchemy/SQLModel
  Infra:     GitHub | GitHub CI/CD | Docker | Vercel/GCP

Special Focus:
  [X] AI integration guidelines   [X] Documentation requirements

======================================== HOW TO USE ========================================

Submit the above request to an AI prompt engineer (e.g., Claude, GPT-4) along with the
full blank meta-prompt template (prompt-generator-blank.md). The AI will generate a
comprehensive architect prompt for creating the frontend template skeleton.

The generated prompt should be placed in: prompts/general/architect/
See: architect/create-frontend-template-skeleton.md for the generated output.
