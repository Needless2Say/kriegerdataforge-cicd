======================================== PROMPT GENERATOR — BACKEND TEMPLATE SKELETON REQUEST ========================================

Prompt Name: Create Template Back End Skeleton

Target AI Role: [X] 🏗️ ARCHITECT

Project/Application Name: Back End Template Skeleton

Brief Description of Project:
The Back End Template Skeleton is a folder that should be a template skeleton of a FastAPI
backend application that can be used to create an application without having to do all the
setup for the fastapi backend application repo.

This template skeleton should use the KriegerDataForge FastAPI application's api/fitness_app/
directory as an example for how to structure the template skeleton so developers can create
other directories inside the FastAPI app to support another frontend application with the same
framework structure.

Any developer should be able to copy this FastAPI backend application template skeleton and
immediately get started on developing their FastAPI backend application.

It should also include examples of how to structure features with example files and other
types of examples to show how to use the template. Some examples should also include the
workflow of creating a route, such as what constants need to be created, what schemas need to
be created, what models need to be created, what service functions need to be created in the
service.py file, what dependencies enums exceptions and more need to be created.

The FastAPI backend template skeleton should be placed in the templates directory.

Scope: [X] 🏢 Enterprise - Organization-wide template for all future back-end application modules

Tech Stack:
  Frontend:  Next.js | TailwindCSS | TypeScript/React | React state
  Backend:   FastAPI | Python/SQL | PostgreSQL | SQLAlchemy/SQLModel
  Infra:     GitHub | GitHub CI/CD | Docker | Vercel/GCP

======================================== HOW TO USE ========================================

Submit the above request to an AI prompt engineer (e.g., Claude, GPT-4) along with the
full blank meta-prompt template (prompt-generator-blank.md). The AI will generate a
comprehensive architect prompt for creating the backend template skeleton.

The generated prompt should be placed in: prompts/general/architect/
See: architect/create-backend-template-skeleton.md for the generated output.
